apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - cert-manager
  - arc               # Actions Runner Controller — self-hosted GitHub runners
  - external-secrets
  - renovate
  - nfs-storage
  - smb-csi
  - gpu-operator
  - k8s-backup
  - system-tuning
  - cloudnative-pg
  - unifi-data
  # - argo-workflows  # Deactivated 2026-08-11 - audio-workflows was its only consumer (sole source of
  #                   # Workflow/CronWorkflow/WorkflowTemplate CRs cluster-wide) and is now retired.
  - system-upgrade-controller
  # - newrelic
