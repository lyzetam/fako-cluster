apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# No namespace transformer: the base manifest declares namespace
# `system-upgrade` for the controller and Plans. The Plan CRs live in the
# same namespace as the controller.
resources:
  - ../../base/system-upgrade-controller/
