# infrastructure/controllers/base/k8s-backup/nfs-backup-pvc.yaml
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: backup-storage
  namespace: backup-system
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 500Gi
  volumeName: k8s-backup-pv  # Updated to use the new PV name
  storageClassName: ""