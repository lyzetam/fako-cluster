apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # Vendored from upstream v0.19.2 — keep in sync with controller.yaml image tag.
  - crd.yaml
  - controller.yaml
  # Our K3s upgrade Plans. Edit the version field here to trigger upgrades.
  - k3s-upgrade-plans.yaml
