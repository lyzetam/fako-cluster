apiVersion: v1
kind: ResourceQuota
metadata:
  name: gpu-quota
  namespace: nvidia-device-plugin
spec:
  hard:
    requests.nvidia.com/gpu: "1"  # Adjust based on your playground node GPU count
    limits.nvidia.com/gpu: "2"