apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: csi-driver-nfs
  namespace: nfs-system
spec:
  interval: 30m
  chart:
    spec:
      chart: csi-driver-nfs
      version: "^4.8.0"
      sourceRef:
        kind: HelmRepository
        name: csi-driver-nfs
        namespace: nfs-system
      interval: 12h
  install:
    createNamespace: true
