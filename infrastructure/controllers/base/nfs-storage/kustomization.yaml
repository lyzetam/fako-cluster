apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  - repository.yaml  
  - release.yaml
  - dynamic-storageclass-job.yaml
  # StorageClasses are created dynamically by the job
  # No hardcoded IPs anywhere in the configuration
