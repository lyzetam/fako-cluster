apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  - repository.yaml  
  - release.yaml
  - nfs-server-secret.yaml
  - storageclass-v3.yaml  # Template-based storage classes  - apply-storageclass-job.yaml  # Job to create storage classes
  - rbac.yaml  # RBAC for the job
