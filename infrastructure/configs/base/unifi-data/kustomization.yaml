apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: infrastructure
resources:
  # aws-secret-store.yaml removed - uses shared SecretStore from nfs-config
  - external-secret-unifi.yaml
