apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: infrastructure

resources:
  - namespace.yaml
  - aws-credentials-secret.yaml
  - aws-secret-store.yaml
  - external-secret-nfs.yaml
