apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # Core applications
  - linkding
  - audiobookshelf
  # - homebot
  - oura-collector
  - oura-dashboard
  - wger

  # Database Management
  - postgres-cluster
  - pgadmin

  # Identity Management
  - keycloak

  # Security Scanning
  - gitleaks
  
  # AI/ML stack with GPU support (excluded from dev due to limited GPU resources)
  # - ollama         # GPU required - disabled in dev
  - ollama-webui    # UI only, no GPU required
  # - whisper        # GPU required - disabled in dev
  # - piper          # GPU required - disabled in dev
  # - openwakeword   # GPU required - disabled in dev
  - voice-monitor   # UI only, no GPU required
  - node-labeling
