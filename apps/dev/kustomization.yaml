apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # Core applications
  - linkding
  - audiobookshelf
  # - homebot
  - oura-collector
  - oura-dashboard
  - wger

  # Database Management
  - postgres-cluster
  - pgadmin

  # Identity Management
  - keycloak

  # Security Scanning
  - gitleaks
  
  # AI/ML stack - CPU versions for dev
  - ollama         # CPU version for dev
  - ollama-webui   # UI only, no GPU required
  - whisper        # CPU version for dev
  - piper          # Already CPU-based
  - openwakeword   # Already CPU-based
  - node-labeling
