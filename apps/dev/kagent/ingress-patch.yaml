apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: kagent
spec:
  rules:
  - host: kagent-dev.landryzetam.net
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: kagent
            port:
              number: 80
