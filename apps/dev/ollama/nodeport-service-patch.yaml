apiVersion: v1
kind: Service
metadata:
  name: ollama-nodeport
  namespace: dev-ollama
spec:
  type: NodePort
  ports:
    - port: 11434
      targetPort: 11434
      nodePort: 31435  # Different port for dev environment
  selector:
    app: ollama-gpu
