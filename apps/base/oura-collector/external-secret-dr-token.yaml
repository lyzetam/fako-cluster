apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: dr-agent-token-env
  namespace: oura-collector
spec:
  secretStoreRef:
    name: aws-secret-store
    kind: SecretStore
  target:
    name: dr-agent-token-env
    creationPolicy: Owner
    template:
      type: Opaque
      data:
        DR_AGENT_TOKEN: "{{ .token }}"
  data:
  - secretKey: token
    remoteRef:
      key: oura-agent/webhook-token
      property: token
  refreshInterval: 1h
