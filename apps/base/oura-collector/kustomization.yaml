apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: oura-collector
resources:
  - namespace.yaml
  - rbac.yaml
  - configmap.yaml
  - deployment.yaml
  - service.yaml
  - ingress.yaml  # Uncomment if you want to expose health endpoint
  # - cronjob.yaml  # Alternative to deployment for scheduled collection