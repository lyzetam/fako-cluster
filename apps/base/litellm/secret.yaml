# apps/base/litellm/secret.yaml
# Note: In production, encrypt this with SOPS before committing!
apiVersion: v1
kind: Secret
metadata:
  name: litellm-secrets
  namespace: litellm
type: Opaque
stringData:
  LITELLM_MASTER_KEY: "sk-1022"  # Must match config.yaml
  LITELLM_SALT_KEY: "sk-IpV6HuAx9DVeDvv"  # Generate a random string
  DATABASE_URL: "sqlite:///./litellm.db"  # Or use PostgreSQL in production
  
  # Dummy values for the exo connection (since exo doesn't need auth)
  OPENAI_API_KEY: "dummy"