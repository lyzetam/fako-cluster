# apps/base/litellm/nodeport-service.yaml
apiVersion: v1
kind: Service
metadata:
  name: litellm-nodeport
  namespace: litellm
  labels:
    app: litellm
spec:
  type: NodePort
  ports:
    - port: 4000
      targetPort: 4000
      nodePort: 32767
      protocol: TCP
  selector:
    app: litellm