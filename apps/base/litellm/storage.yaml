apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-pvc
  namespace: litellm
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: nfs-csi  # Using your NFS storage class
  resources:
    requests:
      storage: 5Gi