apiVersion: batch/v1
kind: Job
metadata:
  name: create-ghcr-registry-secret
  namespace: connor-birthday
spec:
  template:
    spec:
      serviceAccountName: registry-secret-creator
      containers:
      - name: create-secret
        image: bitnami/kubectl:latest
        command:
        - /bin/bash
        - -c
        - |
          USERNAME=$(kubectl get secret ghcr-credentials -n connor-birthday -o jsonpath='{.data.username}' | base64 -d)
          PASSWORD=$(kubectl get secret ghcr-credentials -n connor-birthday -o jsonpath='{.data.password}' | base64 -d)
          kubectl delete secret ghcr-registry -n connor-birthday --ignore-not-found=true
          kubectl create secret docker-registry ghcr-registry \
            --docker-server=ghcr.io \
            --docker-username="${USERNAME}" \
            --docker-password="${PASSWORD}" \
            --namespace=connor-birthday
      restartPolicy: OnFailure
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: registry-secret-creator
  namespace: connor-birthday
---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: registry-secret-creator
  namespace: connor-birthday
rules:
- apiGroups: [""]
  resources: ["secrets"]
  verbs: ["get", "create", "delete"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: registry-secret-creator
  namespace: connor-birthday
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: registry-secret-creator
subjects:
- kind: ServiceAccount
  name: registry-secret-creator
  namespace: connor-birthday
