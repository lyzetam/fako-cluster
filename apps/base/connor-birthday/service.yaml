apiVersion: v1
kind: Service
metadata:
  name: connor-birthday
  namespace: connor-birthday
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: http
    name: http
  selector:
    app: connor-birthday
