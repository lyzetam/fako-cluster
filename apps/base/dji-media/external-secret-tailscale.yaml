# Tailscale auth key (tagged tag:k8s) for the proxy sidecar. From AWS SM.
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: tailscale-authkey
  namespace: dji-media
spec:
  secretStoreRef:
    name: aws-secret-store
    kind: SecretStore
  target:
    name: tailscale-authkey
    creationPolicy: Owner
    template:
      type: Opaque
      data:
        TS_AUTHKEY: "{{ .authkey }}"
  data:
  - secretKey: authkey
    remoteRef:
      key: tailscale/dji-media-authkey
      property: authkey
  refreshInterval: 1h
