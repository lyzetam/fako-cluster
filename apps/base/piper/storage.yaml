# apps/base/piper/pvc.yaml
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: piper-data
  namespace: piper
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: nfs-csi-v3  # Same as your Ollama PVC
  resources:    requests:
      storage: 15Gi  # Voice models need space
