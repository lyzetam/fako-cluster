# apps/base/piper/deployment.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: piper
  namespace: piper
spec:
  replicas: 1
  selector:
    matchLabels:
      app: piper
  template:
    metadata:
      labels:
        app: piper
    spec:
      containers:
        - name: piper
          image: rhasspy/wyoming-piper:1.5.0  # Pin to specific version to avoid issues
          ports:
            - containerPort: 10200
              protocol: TCP
          command:
            - python3
            - -m
            - wyoming_piper
          args:
            - --voice
            - "en_US-amy-medium"
            - --uri
            - "tcp://0.0.0.0:10200"
            - --data-dir
            - "/data"
            - --download-dir
            - "/data"
            - --max-piper-procs
            - "2"
          volumeMounts:
            - mountPath: /data
              name: piper-models
          resources:
            requests:
              memory: "512Mi"
              cpu: "500m"
            limits:
              memory: "1Gi"
              cpu: "1000m"
          # Health checks for Wyoming protocol
          livenessProbe:
            tcpSocket:
              port: 10200
            initialDelaySeconds: 30
            periodSeconds: 30
          readinessProbe:
            tcpSocket:
              port: 10200
            initialDelaySeconds: 10
            periodSeconds: 10
      volumes:
        - name: piper-models
          persistentVolumeClaim:
            claimName: piper-models