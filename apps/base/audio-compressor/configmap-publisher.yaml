apiVersion: v1
kind: ConfigMap
metadata:
  name: transcript-publisher-config
  namespace: audio-compressor
data:
  INPUT_DIR: "/data/transcriptions"
  STATE_DIR: "/data/state"
  OBSIDIAN_FOLDER: "Transcripts/Raw"
  SKIP_PROCESSED: "true"
  LOG_LEVEL: "INFO"
  FILE_PREFIX: "Transcript"
  MAX_RETRIES: "3"
  RETRY_DELAY: "10"
