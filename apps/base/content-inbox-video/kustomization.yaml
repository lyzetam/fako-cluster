apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: content-inbox-video
resources:
  - namespace.yaml
  - secret-store.yaml
  - external-secret.yaml
  - external-secret-sync.yaml
  - external-secret-dockerhub.yaml
  - networkpolicy.yaml
  - nfs-storage.yaml
  - configmap-sync-script.yaml
  - sync-backfill-job.yaml
  - restore-job.yaml
  - backfill-job.yaml
  # One-off, cost-bearing backfill. Enable only after the image is published
  # and the dry-run inventory has been reviewed:
  # - sync-backfill-job.yaml
  # - backfill-job.yaml
