apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: homebot
resources:
  - namespace.yaml
  - configmap.yaml  # ConfigMap must be created before Deployment
  - storage.yaml
  - service.yaml
  - deployment.yaml
  - ingress.yaml