# Patch the namespace's default ServiceAccount so every pod (including
# subchart pods like mariadb, valkey) inherits the DockerHub pull secret.
# Avoids public-rate-limit 429s.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: default
  namespace: amig-erp
imagePullSecrets:
  - name: dockerhub-registry
