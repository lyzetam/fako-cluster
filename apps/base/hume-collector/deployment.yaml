apiVersion: apps/v1
kind: Deployment
metadata:
  name: hume-collector
  namespace: hume-collector
spec:
  replicas: 1
  strategy:
    type: RollingUpdate       # maxSurge 0 → old pod deleted before new one, releasing the RWO PVC
    rollingUpdate:
      maxSurge: 0
      maxUnavailable: 1
  selector:
    matchLabels:
      app: hume-collector
  template:
    metadata:
      labels:
        app: hume-collector
    spec:
      serviceAccountName: hume-collector
      imagePullSecrets:
        - name: dockerhub-pull
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
      containers:
      - name: hume-collector
        image: lzetam/hume-collector:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8080
          name: http
          protocol: TCP
        envFrom:
        - configMapRef:
            name: hume-collector-config
        - secretRef:
            name: postgres-credentials-env
        # Token dropped: endpoint is private to the tailnet (Tailscale). The
        # receiver runs open when HUME_INGEST_TOKEN is unset. Re-add for public
        # (Cloudflare) exposure.
        resources:
          requests:
            memory: "128Mi"
            cpu: "50m"
          limits:
            memory: "256Mi"
            cpu: "300m"
        livenessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 15
          periodSeconds: 30
        readinessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 5
          periodSeconds: 10
        securityContext:
          allowPrivilegeEscalation: false
          readOnlyRootFilesystem: true
          capabilities:
            drop: ["ALL"]

      # Tailscale sidecar — serves the receiver privately over HTTPS on the tailnet.
      - name: tailscale
        image: tailscale/tailscale:v1.80.0
        env:
        - name: TS_AUTHKEY
          valueFrom:
            secretKeyRef:
              name: tailscale-authkey
              key: TS_AUTHKEY
        - name: TS_HOSTNAME
          value: "hume-collector"
        - name: TS_EXTRA_ARGS
          value: "--advertise-tags=tag:k8s"
        - name: TS_USERSPACE
          value: "true"
        - name: TS_KUBE_SECRET
          value: ""                                 # no k8s API (unreachable in userspace mode)
        - name: TS_STATE_DIR
          value: "/tmp/tsstate"                     # persistent dir state on a PVC → stable name
        - name: TS_SOCKET
          value: "/tmp/tsstate/tailscaled.sock"
        - name: TS_SERVE_CONFIG
          value: "/etc/tailscale/serve.json"
        volumeMounts:
        - name: serve
          mountPath: /etc/tailscale
        - name: tsstate
          mountPath: /tmp/tsstate
        resources:
          requests:
            memory: "64Mi"
            cpu: "20m"
          limits:
            memory: "128Mi"
            cpu: "200m"
      volumes:
      - name: serve
        configMap:
          name: tailscale-serve
      - name: tsstate
        persistentVolumeClaim:
          claimName: hume-tailscale-state
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: hume-tailscale-state
  namespace: hume-collector
spec:
  accessModes: ["ReadWriteOnce"]
  storageClassName: nfs-csi-v2
  resources:
    requests:
      storage: 64Mi
