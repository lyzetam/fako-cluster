apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: oura-dashboard
resources:
  - namespace.yaml
  - rbac.yaml
  - rbac-additions.yaml      # New: Additional RBAC for External Secrets
  - secret-store.yaml        # New: Local SecretStore
  - aws-secret-store.yaml    # New: AWS SecretStore for External Secrets
  - external-secret-aws.yaml # New: External Secret for AWS credentials
  - external-secret-oauth2.yaml # New: External Secret for OAuth2 Proxy
  - oauth2-proxy.yaml
  - oauth2-proxy-service.yaml
  - configmap.yaml
  - deployment.yaml
  - service.yaml
  - ingress.yaml
  - ingress-acme.yaml       # ACME challenge ingress without auth
