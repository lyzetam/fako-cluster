apiVersion: v1
kind: Service
metadata:
  name: oura-dashboard
  namespace: oura-dashboard
spec:
  selector:
    app: oura-dashboard
  ports:
  - port: 8501  # Changed from 8080
    targetPort: 8501  # Changed from 8080
    protocol: TCP
    name: http
  type: ClusterIP