apiVersion: external-secrets.io/v1beta1
kind: SecretStore
metadata:
  name: local-secret-store
  namespace: oura-dashboard
spec:
  provider:
    kubernetes:
      remoteNamespace: oura-dashboard
      auth:
        serviceAccount:
          name: oura-dashboard
      server:
        caProvider:
          type: ConfigMap
          name: kube-root-ca.crt
          key: ca.crt