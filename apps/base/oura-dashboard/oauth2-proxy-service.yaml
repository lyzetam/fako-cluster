apiVersion: v1
kind: Service
metadata:
  name: oauth2-proxy
  namespace: oura-dashboard
spec:
  selector:
    app: oauth2-proxy
  ports:
  - port: 4180
    targetPort: 4180
    protocol: TCP
    name: http  type: ClusterIP
