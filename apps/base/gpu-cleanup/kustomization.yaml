# apps/base/gpu-cleanup/kustomization.yaml
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: cleanup-jobs
resources:
  - gpu-cleanup-job.yaml
  # Note: namespace.yaml and rbac.yaml are already created by cleanup-old-gpu
  