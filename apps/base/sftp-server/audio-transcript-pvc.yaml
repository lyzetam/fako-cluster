apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: audio-transcript-pvc
  namespace: sftp-server
  labels:
    app: sftp-server
    purpose: audio-transcripts
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: nfs-csi-v2
  resources:
    requests:
      storage: 100Gi
