apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: sftp-host-keys
  namespace: sftp-server
  labels:
    app: sftp-server
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: nfs-csi-v2
  resources:
    requests:
      storage: 100Mi
