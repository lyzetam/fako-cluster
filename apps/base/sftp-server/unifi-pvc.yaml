apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: unifi-journals
  namespace: sftp-server
  labels:
    app: sftp-server
spec:
  accessModes:
    - ReadOnlyMany
  storageClassName: ""
  volumeName: unifi-journals-pv
  resources:
    requests:
      storage: 500Gi
