apiVersion: v1
kind: Service
metadata:
  name: sftp-server
  namespace: sftp-server
  labels:
    app: sftp-server
spec:
  type: ClusterIP
  ports:
    - port: 22
      targetPort: 22
      protocol: TCP
      name: ssh
  selector:
    app: sftp-server
