apiVersion: v1
kind: Service
metadata:
  name: ai-news-api
  namespace: ai-news
spec:
  selector:
    app: ai-news-api
  ports:
    - port: 80
      targetPort: 8000
      name: http
  type: ClusterIP
