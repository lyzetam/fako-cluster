apiVersion: v1
kind: Service
metadata:
  name: zi-dashboard
  namespace: zi
  labels:
    app: zi-dashboard
spec:
  type: ClusterIP
  selector:
    app: zi-dashboard
  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
