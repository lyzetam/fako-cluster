apiVersion: v1
kind: Service
metadata:
  name: zi
  namespace: autobots
  labels:
    app: zi
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: zi
