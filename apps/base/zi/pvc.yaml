apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: zi-brain-pvc
  namespace: autobots
  labels:
    app: zi
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 10Gi
