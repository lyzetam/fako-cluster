apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: platform-security-memory
  namespace: zi
  labels:
    app.kubernetes.io/name: zi
    app.kubernetes.io/component: agent-memory
    zi.agent: platform-security
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: nfs-client
  resources:
    requests:
      storage: 100Mi
