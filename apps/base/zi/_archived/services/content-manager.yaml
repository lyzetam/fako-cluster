apiVersion: v1
kind: Service
metadata:
  name: agent-content-manager
  namespace: zi
  labels:
    app.kubernetes.io/name: agent-content-manager
    app.kubernetes.io/component: agent
    app.kubernetes.io/part-of: zi-agents
spec:
  selector:
    app: agent-content-manager
  ports:
    - port: 8000
      targetPort: http
      name: http
