apiVersion: v1
kind: Service
metadata:
  name: agent-pr-manager
  namespace: zi
  labels:
    app.kubernetes.io/name: agent-pr-manager
    app.kubernetes.io/component: agent
    app.kubernetes.io/part-of: zi-agents
spec:
  selector:
    app: agent-pr-manager
  ports:
    - port: 8000
      targetPort: http
      name: http
