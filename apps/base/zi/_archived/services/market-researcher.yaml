apiVersion: v1
kind: Service
metadata:
  name: agent-market-researcher
  namespace: zi
  labels:
    app.kubernetes.io/name: agent-market-researcher
    app.kubernetes.io/component: agent
    app.kubernetes.io/part-of: zi-agents
spec:
  selector:
    app: agent-market-researcher
  ports:
    - port: 8000
      targetPort: http
      name: http
