apiVersion: v1
kind: Service
metadata:
  name: agent-vp-demand-generation
  namespace: zi
  labels:
    app.kubernetes.io/name: agent-vp-demand-generation
    app.kubernetes.io/component: agent
    app.kubernetes.io/part-of: zi-agents
spec:
  selector:
    app: agent-vp-demand-generation
  ports:
    - port: 8000
      targetPort: http
      name: http
