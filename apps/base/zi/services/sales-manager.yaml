apiVersion: v1
kind: Service
metadata:
  name: agent-sales-manager
  namespace: zi
  labels:
    app.kubernetes.io/name: agent-sales-manager
    app.kubernetes.io/component: agent
    app.kubernetes.io/part-of: zi-agents
spec:
  selector:
    app: agent-sales-manager
  ports:
    - port: 8000
      targetPort: http
      name: http
