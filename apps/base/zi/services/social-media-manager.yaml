apiVersion: v1
kind: Service
metadata:
  name: agent-social-media-manager
  namespace: zi
  labels:
    app.kubernetes.io/name: agent-social-media-manager
    app.kubernetes.io/component: agent
    app.kubernetes.io/part-of: zi-agents
spec:
  selector:
    app: agent-social-media-manager
  ports:
    - port: 8000
      targetPort: http
      name: http
