apiVersion: v1
kind: Service
metadata:
  name: agent-revenue-operations-manager
  namespace: zi
  labels:
    app.kubernetes.io/name: agent-revenue-operations-manager
    app.kubernetes.io/component: agent
    app.kubernetes.io/part-of: zi-agents
spec:
  selector:
    app: agent-revenue-operations-manager
  ports:
    - port: 8000
      targetPort: http
      name: http
