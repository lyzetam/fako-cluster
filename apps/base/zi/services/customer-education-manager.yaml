apiVersion: v1
kind: Service
metadata:
  name: agent-customer-education-manager
  namespace: zi
  labels:
    app.kubernetes.io/name: agent-customer-education-manager
    app.kubernetes.io/component: agent
    app.kubernetes.io/part-of: zi-agents
spec:
  selector:
    app: agent-customer-education-manager
  ports:
    - port: 8000
      targetPort: http
      name: http
