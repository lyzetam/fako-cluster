apiVersion: v1
kind: Service
metadata:
  name: agent-head-of-design
  namespace: zi
  labels:
    app.kubernetes.io/name: agent-head-of-design
    app.kubernetes.io/component: agent
    app.kubernetes.io/part-of: zi-agents
spec:
  selector:
    app: agent-head-of-design
  ports:
    - port: 8000
      targetPort: http
      name: http
