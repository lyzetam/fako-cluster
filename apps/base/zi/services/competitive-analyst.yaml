apiVersion: v1
kind: Service
metadata:
  name: agent-competitive-analyst
  namespace: zi
  labels:
    app.kubernetes.io/name: agent-competitive-analyst
    app.kubernetes.io/component: agent
    app.kubernetes.io/part-of: zi-agents
spec:
  selector:
    app: agent-competitive-analyst
  ports:
    - port: 8000
      targetPort: http
      name: http
