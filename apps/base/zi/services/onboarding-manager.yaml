apiVersion: v1
kind: Service
metadata:
  name: agent-onboarding-manager
  namespace: zi
  labels:
    app.kubernetes.io/name: agent-onboarding-manager
    app.kubernetes.io/component: agent
    app.kubernetes.io/part-of: zi-agents
spec:
  selector:
    app: agent-onboarding-manager
  ports:
    - port: 8000
      targetPort: http
      name: http
