apiVersion: v1
kind: Service
metadata:
  name: agent-cs-director
  namespace: zi
  labels:
    app.kubernetes.io/name: agent-cs-director
    app.kubernetes.io/component: agent
    app.kubernetes.io/part-of: zi-agents
spec:
  selector:
    app: agent-cs-director
  ports:
    - port: 8000
      targetPort: http
      name: http
