apiVersion: v1
kind: ConfigMap
metadata:
  name: voice-monitor-config
  namespace: voice-monitor
data:
  REFRESH_INTERVAL: "30"
  FLASK_PORT: "8080"
  PROMETHEUS_URL: "http://kube-prometheus-stack-prometheus.monitoring.svc.cluster.local:9090"
  # ENABLE_KUBECTL_TOP: "true"