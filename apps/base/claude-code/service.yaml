apiVersion: v1
kind: Service
metadata:
  name: claude-code
  namespace: claude-code
  labels:
    app: claude-code
spec:
  type: ClusterIP
  ports:
    - port: 22
      targetPort: 22
      protocol: TCP
      name: ssh
  selector:
    app: claude-code
