apiVersion: v1
kind: Service
metadata:
  name: piper-v13
  namespace: piper-v13
spec:
  selector:
    app: piper-v13
  ports:
    - name: wyoming
      port: 10201
      targetPort: 10200
      protocol: TCP
  type: ClusterIP
