apiVersion: v1
kind: Service
metadata:
  name: mcp-gateway
  namespace: mcp-gateway
  labels:
    app.kubernetes.io/name: mcp-gateway
    app.kubernetes.io/component: gateway
spec:
  type: ClusterIP
  ports:
    - port: 8811
      targetPort: 8811
      protocol: TCP
      name: sse
    - port: 8812
      targetPort: 8812
      protocol: TCP
      name: streaming
  selector:
    app.kubernetes.io/name: mcp-gateway
    app.kubernetes.io/component: gateway
