apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: thornus
resources:
  - namespace.yaml
  - configmap.yaml
  # ExternalSecret disabled - thornus-secrets managed manually
  # Re-enable when SOPS key is available for proper secret encryption
  # - secret-store.yaml
  # - external-secret.yaml
  - backend-deployment.yaml
  - frontend-deployment.yaml
  - services.yaml
  - ingress.yaml
