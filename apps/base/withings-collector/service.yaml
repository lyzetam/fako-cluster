apiVersion: v1
kind: Service
metadata:
  name: withings-collector
  namespace: withings-collector
spec:
  selector:
    app: withings-collector
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http
  type: ClusterIP
