apiVersion: v1
kind: Service
metadata:
  name: discord-mcp
  namespace: discord-mcp
  labels:
    app: discord-mcp
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
  selector:
    app: discord-mcp
