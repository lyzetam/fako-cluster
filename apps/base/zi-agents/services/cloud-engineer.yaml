apiVersion: v1
kind: Service
metadata:
  name: agent-cloud-engineer
  namespace: zi
  labels:
    app.kubernetes.io/name: agent-cloud-engineer
    app.kubernetes.io/component: agent
    app.kubernetes.io/part-of: zi-agents
spec:
  selector:
    app: agent-cloud-engineer
  ports:
    - port: 8000
      targetPort: http
      name: http
