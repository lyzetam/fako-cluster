apiVersion: v1
kind: Service
metadata:
  name: agent-tech-writer
  namespace: zi
  labels:
    app.kubernetes.io/name: agent-tech-writer
    app.kubernetes.io/component: agent
    app.kubernetes.io/part-of: zi-agents
spec:
  selector:
    app: agent-tech-writer
  ports:
    - port: 8000
      targetPort: http
      name: http
