apiVersion: v1
kind: Service
metadata:
  name: agent-enterprise-csm
  namespace: zi
  labels:
    app.kubernetes.io/name: agent-enterprise-csm
    app.kubernetes.io/component: agent
    app.kubernetes.io/part-of: zi-agents
spec:
  selector:
    app: agent-enterprise-csm
  ports:
    - port: 8000
      targetPort: http
      name: http
