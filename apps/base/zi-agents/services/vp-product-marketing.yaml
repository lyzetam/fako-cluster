apiVersion: v1
kind: Service
metadata:
  name: agent-vp-product-marketing
  namespace: zi
  labels:
    app.kubernetes.io/name: agent-vp-product-marketing
    app.kubernetes.io/component: agent
    app.kubernetes.io/part-of: zi-agents
spec:
  selector:
    app: agent-vp-product-marketing
  ports:
    - port: 8000
      targetPort: http
      name: http
