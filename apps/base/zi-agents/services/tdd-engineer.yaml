apiVersion: v1
kind: Service
metadata:
  name: agent-tdd-engineer
  namespace: zi-agents
  labels:
    app.kubernetes.io/name: agent-tdd-engineer
    app.kubernetes.io/component: agent
    app.kubernetes.io/part-of: zi-agents
spec:
  selector:
    app: agent-tdd-engineer
  ports:
    - port: 8000
      targetPort: http
      name: http
