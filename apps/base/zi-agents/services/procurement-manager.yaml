apiVersion: v1
kind: Service
metadata:
  name: agent-procurement-manager
  namespace: zi
  labels:
    app.kubernetes.io/name: agent-procurement-manager
    app.kubernetes.io/component: agent
    app.kubernetes.io/part-of: zi-agents
spec:
  selector:
    app: agent-procurement-manager
  ports:
    - port: 8000
      targetPort: http
      name: http
