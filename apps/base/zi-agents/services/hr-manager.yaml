apiVersion: v1
kind: Service
metadata:
  name: agent-hr-manager
  namespace: zi
  labels:
    app.kubernetes.io/name: agent-hr-manager
    app.kubernetes.io/component: agent
    app.kubernetes.io/part-of: zi-agents
spec:
  selector:
    app: agent-hr-manager
  ports:
    - port: 8000
      targetPort: http
      name: http
