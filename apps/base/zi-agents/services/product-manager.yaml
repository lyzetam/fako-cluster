apiVersion: v1
kind: Service
metadata:
  name: agent-product-manager
  namespace: zi
  labels:
    app.kubernetes.io/name: agent-product-manager
    app.kubernetes.io/component: agent
    app.kubernetes.io/part-of: zi-agents
spec:
  selector:
    app: agent-product-manager
  ports:
    - port: 8000
      targetPort: http
      name: http
