apiVersion: v1
kind: Service
metadata:
  name: agent-ux-designer
  namespace: zi
  labels:
    app.kubernetes.io/name: agent-ux-designer
    app.kubernetes.io/component: agent
    app.kubernetes.io/part-of: zi-agents
spec:
  selector:
    app: agent-ux-designer
  ports:
    - port: 8000
      targetPort: http
      name: http
