apiVersion: v1
kind: Service
metadata:
  name: agent-communications-director
  namespace: zi
  labels:
    app.kubernetes.io/name: agent-communications-director
    app.kubernetes.io/component: agent
    app.kubernetes.io/part-of: zi-agents
spec:
  selector:
    app: agent-communications-director
  ports:
    - port: 8000
      targetPort: http
      name: http
