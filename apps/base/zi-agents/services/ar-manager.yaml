apiVersion: v1
kind: Service
metadata:
  name: agent-ar-manager
  namespace: zi
  labels:
    app.kubernetes.io/name: agent-ar-manager
    app.kubernetes.io/component: agent
    app.kubernetes.io/part-of: zi-agents
spec:
  selector:
    app: agent-ar-manager
  ports:
    - port: 8000
      targetPort: http
      name: http
