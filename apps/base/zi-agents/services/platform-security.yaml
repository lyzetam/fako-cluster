apiVersion: v1
kind: Service
metadata:
  name: agent-platform-security
  namespace: zi
  labels:
    app.kubernetes.io/name: agent-platform-security
    app.kubernetes.io/component: agent
    app.kubernetes.io/part-of: zi-agents
spec:
  selector:
    app: agent-platform-security
  ports:
    - port: 8000
      targetPort: http
      name: http
