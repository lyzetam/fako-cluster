apiVersion: v1
kind: Service
metadata:
  name: agent-frontend-engineer
  namespace: zi-agents
  labels:
    app.kubernetes.io/name: agent-frontend-engineer
    app.kubernetes.io/component: agent
    app.kubernetes.io/part-of: zi-agents
spec:
  selector:
    app: agent-frontend-engineer
  ports:
    - port: 8000
      targetPort: http
      name: http
