apiVersion: v1
kind: Service
metadata:
  name: agent-debugger
  namespace: zi-agents
  labels:
    app.kubernetes.io/name: agent-debugger
    app.kubernetes.io/component: agent
    app.kubernetes.io/part-of: zi-agents
spec:
  selector:
    app: agent-debugger
  ports:
    - port: 8000
      targetPort: http
      name: http
