apiVersion: v1
kind: Service
metadata:
  name: agent-creative-director
  namespace: zi
  labels:
    app.kubernetes.io/name: agent-creative-director
    app.kubernetes.io/component: agent
    app.kubernetes.io/part-of: zi-agents
spec:
  selector:
    app: agent-creative-director
  ports:
    - port: 8000
      targetPort: http
      name: http
