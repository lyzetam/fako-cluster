apiVersion: v1
kind: Service
metadata:
  name: agent-tech-lead
  namespace: zi-agents
  labels:
    app.kubernetes.io/name: agent-tech-lead
    app.kubernetes.io/component: agent
    app.kubernetes.io/part-of: zi-agents
spec:
  selector:
    app: agent-tech-lead
  ports:
    - port: 8000
      targetPort: http
      name: http
