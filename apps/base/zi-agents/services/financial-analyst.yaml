apiVersion: v1
kind: Service
metadata:
  name: agent-financial-analyst
  namespace: zi
  labels:
    app.kubernetes.io/name: agent-financial-analyst
    app.kubernetes.io/component: agent
    app.kubernetes.io/part-of: zi-agents
spec:
  selector:
    app: agent-financial-analyst
  ports:
    - port: 8000
      targetPort: http
      name: http
