apiVersion: v1
kind: Service
metadata:
  name: agent-sdr-manager
  namespace: zi
  labels:
    app.kubernetes.io/name: agent-sdr-manager
    app.kubernetes.io/component: agent
    app.kubernetes.io/part-of: zi-agents
spec:
  selector:
    app: agent-sdr-manager
  ports:
    - port: 8000
      targetPort: http
      name: http
