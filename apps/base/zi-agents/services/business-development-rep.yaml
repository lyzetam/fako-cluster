apiVersion: v1
kind: Service
metadata:
  name: agent-business-development-rep
  namespace: zi
  labels:
    app.kubernetes.io/name: agent-business-development-rep
    app.kubernetes.io/component: agent
    app.kubernetes.io/part-of: zi-agents
spec:
  selector:
    app: agent-business-development-rep
  ports:
    - port: 8000
      targetPort: http
      name: http
