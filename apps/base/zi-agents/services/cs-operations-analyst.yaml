apiVersion: v1
kind: Service
metadata:
  name: agent-cs-operations-analyst
  namespace: zi
  labels:
    app.kubernetes.io/name: agent-cs-operations-analyst
    app.kubernetes.io/component: agent
    app.kubernetes.io/part-of: zi-agents
spec:
  selector:
    app: agent-cs-operations-analyst
  ports:
    - port: 8000
      targetPort: http
      name: http
