apiVersion: v1
kind: Service
metadata:
  name: agent-vp-product
  namespace: zi
  labels:
    app.kubernetes.io/name: agent-vp-product
    app.kubernetes.io/component: agent
    app.kubernetes.io/part-of: zi-agents
spec:
  selector:
    app: agent-vp-product
  ports:
    - port: 8000
      targetPort: http
      name: http
