apiVersion: v1
kind: Service
metadata:
  name: open-webui
  namespace: open-webui
spec:
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
  selector:
    app: open-webui
  type: ClusterIP
