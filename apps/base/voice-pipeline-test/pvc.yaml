apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: voice-test-audio
  namespace: voice-pipeline-test
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
  storageClassName: nfs-client  # Adjust if you use a different storage class
