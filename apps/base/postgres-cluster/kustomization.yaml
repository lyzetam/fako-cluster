apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: postgres
resources:
  - namespace.yaml
  - configmap.yaml
  - secret-store.yaml
  - external-secret-admin.yaml    
  - external-secret-app.yaml
  - aws-credentials-secret.yaml
  - postgres-cluster.yaml
  - database-init-job.yaml

  # - service.yaml
  # - backup-schedule.yaml  # Uncomment if using backups
  # - pooler.yaml  # Uncomment if using PgBouncer
