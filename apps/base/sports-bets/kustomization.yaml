apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: sports-bets
resources:
  - namespace.yaml
  - secret-store.yaml
  - external-secret.yaml
  - external-secret-dockerhub.yaml
  - pvc.yaml
  - cronjob.yaml
  - cronjob-agent.yaml
  - cronjob-scoreboard.yaml
  - cronjob-predictions.yaml
  - web.yaml
  - cloudflare.yaml
# aws-credentials + tunnel-credentials (SOPS-encrypted) live in the staging overlay.
