apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: claude-usage-dashboard
resources:
  - namespace.yaml
  - secret-store.yaml
  - external-secret.yaml
  - external-secret-dockerhub.yaml
  - deployment.yaml
  - ingress.yaml
  - networkpolicy.yaml
# aws-credentials (SOPS-encrypted) lives in the staging overlay.
