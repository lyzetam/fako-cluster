apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: memory-data
  namespace: mcp-servers
  labels:
    app.kubernetes.io/name: memory-mcp-server
    app.kubernetes.io/component: mcp-server
    app.kubernetes.io/part-of: mcp-servers
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: local-path
