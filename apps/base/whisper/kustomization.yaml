# apps/base/whisper/kustomization.yaml
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: whisper
resources:
  - namespace.yaml
  # GPU configuration (disabled until GPU node is available)
  # - configmap.yaml
  # - deployment-gpu.yaml
  # - service-gpu.yaml
  
  # CPU configuration (enabled)
  - configmap-cpu.yaml
  - deployment-cpu.yaml
  - service-cpu.yaml
  
  # Jetson configuration (disabled)
  # - configmap-jetson.yaml
  # - deployment-jetson.yaml
  
  # Common resources
  - health-check-configmap.yaml
  - storage.yaml
  - nodeport-service.yaml
