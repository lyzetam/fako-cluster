# apps/base/whisper/kustomization.yaml
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: whisper
resources:
  - namespace.yaml
  - configmap.yaml
  - configmap-jetson.yaml  # Jetson-specific config
  - health-check-configmap.yaml
  - storage.yaml
  - deployment-gpu.yaml  # Using GPU deployment on lenovo node
  # - deployment-jetson.yaml  # Disabled - using GPU deployment instead
  - service-gpu.yaml
  - nodeport-service.yaml
