# apps/base/whisper/kustomization.yaml
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: whisper
resources:
  - namespace.yaml
  # GPU configuration (enabled on aitower node)
  - configmap.yaml
  - deployment-gpu.yaml
  - service-gpu.yaml
  
  
  # Common resources
  - storage.yaml
  - nodeport-service.yaml
