apiVersion: v1
kind: Service
metadata:
  name: whisper-optimized
  namespace: whisper
spec:
  ports:
    - port: 10300
      targetPort: 10300
      protocol: TCP
  selector:
    app: whisper-optimized
  type: ClusterIP