# apps/base/whisper/service-gpu.yaml
apiVersion: v1
kind: Service
metadata:
  name: whisper-gpu
  namespace: whisper
spec:
  ports:
    - port: 10300
      targetPort: 10300
      protocol: TCP
      name: wyoming
  selector:
    app: whisper-gpu
  type: ClusterIP