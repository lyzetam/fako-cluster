apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: gpustack-proxy

resources:
  - namespace.yaml
  - aws-secret-store.yaml
  - external-secret-endpoint.yaml
  - service.yaml
  - endpoint-sync-job.yaml
  - ingress.yaml
