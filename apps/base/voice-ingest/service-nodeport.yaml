apiVersion: v1
kind: Service
metadata:
  name: voice-ingest-nodeport
  namespace: voice-ingest
  labels:
    app: voice-ingest
spec:
  type: NodePort
  ports:
  - port: 8080
    targetPort: 8080
    nodePort: 30080
    protocol: TCP
    name: http
  selector:
    app: voice-ingest
