apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: postgres-superuser-external-secret
  namespace: wger
spec:
  secretStoreRef:
    name: aws-secrets-store
    kind: SecretStore
  target:
    name: postgres-cluster-superuser  # This is what the job expects
    creationPolicy: Owner
    template:
      type: kubernetes.io/basic-auth
  data:
  - secretKey: username
    remoteRef:
      key: postgres/admin-credentials  # Same AWS secret as postgres cluster uses
      property: username
  - secretKey: password
    remoteRef:
      key: postgres/admin-credentials
      property: password
  refreshInterval: 1h