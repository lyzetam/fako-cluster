# apps/base/ollama/nodeport-service.yaml
apiVersion: v1
kind: Service
metadata:
  name: ollama-nodeport
  namespace: ollama
spec:
  type: NodePort
  ports:
    - port: 11434
      targetPort: 11434
      nodePort: 31434
  selector:
    app: ollama-gpu  # Fixed to match GPU deployment