# apps/base/ollama/deployment.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ollama
  namespace: ollama
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ollama
  template:
    metadata:
      labels:
        app: ollama
    spec:
      containers:
        # Main Ollama container
        - name: ollama
          image: ollama/ollama:latest
          ports:
            - containerPort: 11434
              protocol: TCP
          envFrom:
            - configMapRef:
                name: ollama-configmap
          volumeMounts:
            - mountPath: /root/.ollama
              name: ollama-models
          resources:
            requests:
              memory: "6Gi"
              cpu: "2000m"
            limits:
              memory: "12Gi"
              cpu: "4000m"
          # Health checks
          livenessProbe:
            httpGet:
              path: /api/tags
              port: 11434
            initialDelaySeconds: 60
            periodSeconds: 30
            timeoutSeconds: 10
          readinessProbe:
            httpGet:
              path: /api/tags
              port: 11434
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 5
        
        # Sidecar: Model downloader/manager
        - name: model-manager
          image: ollama/ollama:latest
          command: ["/bin/sh"]
          args:
            - -c
            - |
              echo "Model manager starting..."
              sleep 30  # Wait for main Ollama to be ready
              
              # Set Ollama host to connect to main container
              export OLLAMA_HOST=http://localhost:11434
              
              # Download models using Ollama CLI
              while true; do
                echo "Checking if models need to be downloaded..."
                
                # Get current models list
                CURRENT_MODELS=$(ollama list 2>/dev/null || echo "")
                
                # Check if tinyllama exists
                if ! echo "$CURRENT_MODELS" | grep -q "tinyllama"; then
                  echo "Downloading tinyllama..."
                  ollama pull tinyllama && echo "✅ tinyllama downloaded" || echo "❌ tinyllama failed"
                fi
                
                # Check if llama2 exists  
                if ! echo "$CURRENT_MODELS" | grep -q "llama2:7b-chat"; then
                  echo "Downloading llama2:7b-chat..."
                  ollama pull llama2:7b-chat && echo "✅ llama2:7b-chat downloaded" || echo "❌ llama2:7b-chat failed"
                fi
                
                # Show final model list
                echo "Current models:"
                ollama list || echo "Failed to list models"
                
                echo "Model check complete. Sleeping for 10 minutes..."
                sleep 600  # Check every 10 minutes
              done
          volumeMounts:
            - mountPath: /root/.ollama
              name: ollama-models
          resources:
            requests:
              memory: "1Gi"
              cpu: "500m"
            limits:
              memory: "2Gi"
              cpu: "1000m"
      volumes:
        - name: ollama-models
          persistentVolumeClaim:
            claimName: ollama-models