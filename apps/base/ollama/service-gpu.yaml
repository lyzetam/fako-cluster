# apps/base/ollama/service-gpu.yaml
apiVersion: v1
kind: Service
metadata:
  name: ollama-gpu
  namespace: ollama
spec:
  ports:
    - port: 11434
      targetPort: 11434
      protocol: TCP
  selector:
    app: ollama-gpu  type: ClusterIP
