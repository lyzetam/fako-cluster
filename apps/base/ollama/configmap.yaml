apiVersion: v1
kind: ConfigMap
metadata:
  name: ollama-configmap
  namespace: ollama
data:
  OLLAMA_HOST: "0.0.0.0"  # Listen on all interfaces
  OLLAMA_ORIGINS: "*"     # Allow all origins (restrict in production)
  OLLAMA_MODELS: "/models" # Model storage path