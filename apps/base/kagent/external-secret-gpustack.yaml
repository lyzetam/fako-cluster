apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: gpustack-api-secret
  namespace: kagent
spec:
  secretStoreRef:
    name: aws-secret-store
    kind: SecretStore
  target:
    name: kagent-openai-secret
    creationPolicy: Owner
    template:
      type: Opaque
      data:
        OPENAI_API_KEY: "{{ .apiKey }}"
  data:
  - secretKey: apiKey
    remoteRef:
      key: gpustack/api-key
      property: OPENAI_API_KEYS
  refreshInterval: 1h
