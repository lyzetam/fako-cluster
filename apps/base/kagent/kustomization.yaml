apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: kagent

resources:
  - namespace.yaml
  - rbac.yaml
  - aws-secret-store.yaml
  - external-secret-gpustack.yaml
  - external-secret-endpoints.yaml
  - configmap.yaml
  - deployment.yaml
  - service.yaml
  - ingress.yaml

commonLabels:
  app.kubernetes.io/name: kagent
  app.kubernetes.io/part-of: kagent
