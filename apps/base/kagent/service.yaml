apiVersion: v1
kind: Service
metadata:
  name: kagent
  namespace: kagent
  labels:
    app: kagent
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    app: kagent
