apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: kagent-crds
  namespace: kagent
spec:
  interval: 10m
  chart:
    spec:
      chart: kagent-crds
      version: "*"
      sourceRef:
        kind: HelmRepository
        name: kagent-crds
