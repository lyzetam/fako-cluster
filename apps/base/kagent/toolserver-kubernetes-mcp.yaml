apiVersion: kagent.dev/v1alpha1
kind: ToolServer
metadata:
  name: kubernetes-mcp-server
  namespace: kagent
spec:
  description: Kubernetes cluster operations via MCP
  config:
    sse:
      url: http://kubernetes-mcp-server.mcp-servers.svc.cluster.local/sse
