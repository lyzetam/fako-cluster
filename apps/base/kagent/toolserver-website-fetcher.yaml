apiVersion: kagent.dev/v1alpha1
kind: ToolServer
metadata:
  name: mcp-website-fetcher
  namespace: kagent
spec:
  description: Fetch and process web content from URLs
  config:
    sse:
      url: http://mcp-website-fetcher.mcp-servers.svc.cluster.local/sse
