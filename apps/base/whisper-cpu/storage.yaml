apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: whisper-cpu-models
  namespace: whisper-cpu
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: nfs-csi-v2
  resources:
    requests:
      storage: 10Gi  # Whisper models can be large
