# apps/base/whisper-cpu/kustomization.yaml
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: whisper-cpu
resources:
  - namespace.yaml
  - configmap.yaml
  - deployment.yaml
  - service.yaml
  - nodeport-service.yaml
  - storage.yaml
  - dockerhub-secret.yaml
  # Note: create-docker-registry-secret-job.yaml removed due to chicken-and-egg problem
  # Use bootstrap-docker-secret.sh script instead after namespace is deployed
