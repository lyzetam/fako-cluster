# apps/base/whisper-cpu/configmap.yaml
apiVersion: v1
kind: ConfigMap
metadata:
  name: whisper-cpu-configmap
  namespace: whisper-cpu
data:
  # faster-whisper-server configuration
  # Model and inference settings are in deployment env vars
  # This configmap kept for potential future use
  PLACEHOLDER: "unused"
