apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-backup-recovery
  namespace: postgres-recovery
spec:
  accessModes:
    - ReadOnlyMany
  storageClassName: nfs-backup
  volumeName: postgres-backup-recovery-pv
  resources:
    requests:
      storage: 100Gi
