apiVersion: v1
kind: Service
metadata:
  name: postgres-recovery-service
  namespace: postgres-recovery
  labels:
    app: postgres-recovery
spec:
  type: ClusterIP
  ports:
  - port: 5432
    targetPort: 5432
    protocol: TCP
    name: postgresql
  selector:
    app: postgres-recovery
