apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: housekeeping
rules:
- apiGroups: [""]
  resources: ["pods"]
  verbs: ["list", "get", "delete"]
- apiGroups: [""]
  resources: ["namespaces"]
  verbs: ["list", "get"]
