apiVersion: v1
kind: Service
metadata:
  name: auth-service
spec:
  selector:
    app: auth-service
  ports:
    - port: 8501
      targetPort: 8501
      protocol: TCP
      name: streamlit
  type: ClusterIP