# NOTE: This file must be encrypted with SOPS before committing
# Run: sops -e --age age1wz30c9m30hshgx9tg5lylctp646hgu3p0v0sxjuy8xxz34exh4hstn9utd aws-credentials-secret.yaml > aws-credentials-secret.yaml.enc
# Then: mv aws-credentials-secret.yaml.enc aws-credentials-secret.yaml
apiVersion: v1
kind: Secret
metadata:
  name: aws-credentials
  namespace: ${{ values.name }}
type: Opaque
stringData:
  access-key-id: PLACEHOLDER_ACCESS_KEY_ID
  secret-access-key: PLACEHOLDER_SECRET_ACCESS_KEY
  region: us-east-1
