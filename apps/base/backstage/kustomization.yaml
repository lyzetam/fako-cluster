apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: backstage
resources:
  # Core resources
  - namespace.yaml
  - repository.yaml
  - release.yaml
  - ingress.yaml
  - rbac.yaml
  - configmap-app-config.yaml

  # Secrets management
  - aws-credentials-secret.yaml
  - secret-store.yaml
  - external-secret-postgres.yaml
  - external-secret-github.yaml

  # Catalog (for GitHub provider to discover)
  # Note: catalog-info.yaml files are read directly from GitHub
