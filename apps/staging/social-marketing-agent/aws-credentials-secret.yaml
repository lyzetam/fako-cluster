apiVersion: v1
kind: Secret
metadata:
  name: aws-credentials
  namespace: social-marketing-agent
type: Opaque
stringData:
  # TODO: These values need to be encrypted with SOPS before committing
  # Run: sops -e -i aws-credentials-secret.yaml
  access-key-id: PLACEHOLDER_ACCESS_KEY_ID
  secret-access-key: PLACEHOLDER_SECRET_ACCESS_KEY
  region: us-east-1
