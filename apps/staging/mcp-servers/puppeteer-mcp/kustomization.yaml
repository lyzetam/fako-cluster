apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: mcp-servers
resources:
  - ../../../base/mcp-servers/puppeteer-mcp

patches:
  - path: deployment-patch.yaml
    target:
      kind: Deployment
      name: puppeteer-mcp-server
