apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base/mcp-servers
  - n8n-mcp-auth-secret.yaml

patches:
  - path: puppeteer-mcp-deployment-patch.yaml
    target:
      kind: Deployment
      name: puppeteer-mcp-server
