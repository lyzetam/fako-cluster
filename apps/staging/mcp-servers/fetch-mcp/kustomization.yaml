apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../../base/mcp-servers/fetch-mcp

# Add staging-specific patches here if needed
# patches:
#   - path: deployment-patch.yaml
#     target:
#       kind: Deployment
#       name: fetch-mcp-server
