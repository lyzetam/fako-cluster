apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# NOTE: do NOT set a top-level `namespace:` here — the base includes 40
# per-namespace RoleBindings (admin-rolebindings.yaml). A namespace transformer
# would rewrite them all and produce ID conflicts.

resources:
  - ../../base/headlamp
