apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: withings-collector
resources:
  # SOPS-encrypted bootstrap AWS creds for this namespace — decrypted by Flux's
  # sops-age decryption (see ../../base/withings-collector/DEPLOY.md). Mirrors
  # oura/hume-collector's overlay.
  - aws-credentials-secret.yaml
  - cloudflare-secret.yaml
  - ../../base/withings-collector/
