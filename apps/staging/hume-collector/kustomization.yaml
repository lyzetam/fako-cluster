apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: hume-collector
resources:
  # SOPS-encrypted bootstrap AWS creds for this namespace — create with sops
  # (see ../../base/hume-collector/DEPLOY.md). Mirrors oura-collector's overlay.
  - aws-credentials-secret.yaml
  - ../../base/hume-collector/
