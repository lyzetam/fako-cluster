# This is a template - encrypt with SOPS before committing
apiVersion: v1
kind: Secret
metadata:
  name: litellm-secrets
  namespace: litellm
type: Opaque
stringData:
  # Master key for LiteLLM API authentication
  LITELLM_MASTER_KEY: "sk-IpV6HuAx9DVeDvv"
  
  # Add any API keys for external providers here
  # OPENAI_API_KEY: "sk-..."
  # ANTHROPIC_API_KEY: "sk-ant-..."
  
  # Database password if using PostgreSQL
  # DATABASE_URL: "postgresql://user:password@host:5432/litellm"