apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: openwakeword
resources:
  - ../../base/openwakeword/

patches:
  - path: patch-nodeselector.yaml
    target:
      kind: Deployment
      name: openwakeword
