apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: ig-inbox-uploader
resources:
  # SOPS-encrypted bootstrap AWS creds for this namespace — NOT YET CREATED.
  # Mirrors withings-collector's overlay: add `aws-credentials-secret.yaml` here
  # (same AWS creds, namespace: ig-inbox-uploader, re-encrypted with the cluster
  # age key — see apps/base/withings-collector/DEPLOY.md step 5) before this
  # kustomization can actually reconcile.
  # - aws-credentials-secret.yaml
  - ../../base/ig-inbox-uploader/
