# NOTE: This file needs to be encrypted with SOPS before committing
# Run: sops --encrypt --in-place apps/staging/oura-agent/aws-credentials-secret.yaml
#
# Or copy from oura-collector and change namespace:
# sops -d apps/staging/oura-collector/aws-credentials-secret.yaml | \
#   sed 's/namespace: oura-collector/namespace: oura-agent/' | \
#   sops --encrypt --age age1wz30c9m30hshgx9tg5lylctp646hgu3p0v0sxjuy8xxz34exh4hstn9utd \
#   --encrypted-regex '^(data|stringData)$' --input-type yaml --output-type yaml /dev/stdin > \
#   apps/staging/oura-agent/aws-credentials-secret.yaml

apiVersion: v1
kind: Secret
metadata:
    name: aws-credentials
    namespace: oura-agent
type: Opaque
stringData:
    access-key-id: YOUR_AWS_ACCESS_KEY_ID
    secret-access-key: YOUR_AWS_SECRET_ACCESS_KEY
    region: us-east-1
