apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ollama-jetson-models
  namespace: ollama
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: nfs-csi-v2
  resources:
    requests:
      storage: 50Gi  # Smaller storage for Jetson models
