apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: dji-media
resources:
  # The AWS bootstrap creds for this namespace are created imperatively (they
  # cannot live in Git unencrypted), mirroring how ig-inbox-uploader's were.
  - ../../base/dji-media/
