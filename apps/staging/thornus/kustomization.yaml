apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: thornus
resources:
  - ../../base/thornus
  # SOPS secrets disabled - credentials managed manually via kubectl
  # until SOPS age key is configured locally for re-encryption
  # - aws-credentials-secret.yaml
  # - dockerhub-secret.yaml
  # - create-docker-registry-secret-job.yaml
