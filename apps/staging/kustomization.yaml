apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - linkding
  - audiobookshelf
  - ollama
  - ollama-webui
  - whisper
  - piper
  - openwakeword
  - voice-monitor
  # - homebot
  
  # GPU diagnostics and verification
  - verify-gpu-node
  - gpu-diagnostics
  - gpu-status
  - gpu-test
  
  # Patches to fix node names from pgbee to playground
patchesStrategicMerge:
  - playground-node-patch.yaml