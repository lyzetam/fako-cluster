apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base/pixie-cloud

# Optional: Use a custom domain instead of dev.withpixie.dev
# patchesStrategicMerge:
#   - patch-custom-domain.yaml

# Optional: Resource adjustments for your cluster
# patches:
#   - patch: |
#       apiVersion: apps/v1
#       kind: Deployment
#       metadata:
#         name: ".*"
#         namespace: plc
#       spec:
#         template:
#           spec:
#             containers:
#             - name: ".*"
#               resources:
#                 requests:
#                   memory: 128Mi
#                   cpu: 50m
#                 limits:
#                   memory: 256Mi
#                   cpu: 200m
#     target:
#       kind: Deployment
#       namespace: plc
