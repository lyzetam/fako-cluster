apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  - aws-credentials-secret.yaml
  - aws-secret-store.yaml
  - external-secret-license.yaml
  - release.yaml
  # - instrumentation.yaml  # Temporarily disabled - CRD needs to be created first by k8s-agents-operator
