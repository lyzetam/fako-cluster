apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  - aws-credentials-secret.yaml
  - aws-secret-store.yaml
  - external-secret-license.yaml
  - release.yaml
  - repository.yaml

# Set namespace for resources except HelmRepository
namespace: nrdot-collector
