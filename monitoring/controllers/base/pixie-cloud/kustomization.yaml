apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: pixie-cloud-base

resources:
  - namespace.yaml

# Note: Pixie Cloud components need to be deployed manually first
# See setup instructions in ../../../notes/docs/pixie-self-hosted-setup.md
