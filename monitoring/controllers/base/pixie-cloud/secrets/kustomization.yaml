apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - tls-secrets.yaml
  - auth-secrets.yaml
  - pixie-secrets.yaml

# These secrets will be created by the create-secrets job
# based on templates and generated values
